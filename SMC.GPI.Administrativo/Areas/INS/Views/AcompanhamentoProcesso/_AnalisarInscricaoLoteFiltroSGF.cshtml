@using SMC.Formularios.UI.Mvc.Controls
@model AnaliseInscricaoLoteFiltroViewModel

@if (Model.FiltroSGF != null && Model.FiltroSGF.SeqsFormularios.SMCAny())
{
    @Smc.SGFFilter(x => x.FiltroSGF).SeqFilter(Model.SeqProcesso)
} 